schema_version: 1

context:
  version: "0.1.3"

package:
  name: jupyverse-notebook
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-notebook/jupyverse_notebook-${{ version }}.tar.gz
    sha256: 38ce57d9f967a8ea98510d12d7055cf57b56902b709ebbdd065affacfdee9585

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - fastapi-core
    - jupyverse-auth >=0.2.0,<0.3.0
    - jupyverse-api >=0.15.0,<0.16.0

tests:
  - python:
      imports: jupyverse_notebook
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/notebook
  summary: The API for Jupyverse's Notebook.

extra:
  recipe-maintainers:
    - davidbrochart
